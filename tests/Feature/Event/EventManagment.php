<?php

test('organizer can store an event', function () {});

test('event shows is visible to guest users when is published', function () {});

test('event shows in featured page if is featured', function () {});
